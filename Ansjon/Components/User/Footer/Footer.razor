@using MudBlazor

<footer class="footer">
    <div class="footer__inner">
        
        <!-- Vänster: Länkar -->
        <div class="footer__links">
            <FooterLinks />
        </div>

        <!-- Höger: Liten text / logotyp -->
        <div class="footer__brand">
            <span class="footer__brand-name">Ansjon</span>
            <span class="footer__brand-separator">·</span>
            <span class="footer__brand-tagline">en plats för oss</span>
        </div>

    </div>

    <!-- Längst ner: Copyright -->
    <div class="footer__bottom">
        <FooterBottom />
    </div>
</footer>

<style>
    .footer {
        margin-top: 48px;
        padding: 32px 24px 16px 24px;
        border-top: 1px solid #e9edf2;
        background-color: #fafcff;
        color: #6b7084;
        font-size: 0.9rem;
    }

    .footer__inner {
        max-width: 1200px;
        margin: 0 auto;
        display: flex;
        flex-wrap: wrap;
        justify-content: space-between;
        align-items: center;
        gap: 16px 24px;
    }

    .footer__links {
        display: flex;
        flex-wrap: wrap;
        gap: 8px 20px;
    }

    .footer__brand {
        display: flex;
        align-items: center;
        gap: 6px;
        font-size: 0.85rem;
        color: #8a8fa3;
    }

    .footer__brand-name {
        font-weight: 500;
        color: #1a1a2e;
        letter-spacing: 0.3px;
    }

    .footer__brand-separator {
        color: #d0d4dd;
    }

    .footer__brand-tagline {
        font-weight: 300;
        font-style: italic;
    }

    .footer__bottom {
        max-width: 1200px;
        margin: 16px auto 0 auto;
        padding-top: 12px;
        border-top: 1px solid #e9edf2;
        text-align: center;
        font-size: 0.75rem;
        color: #a8adb8;
    }

    
        .footer__brand {
            font-size: 0.8rem;
        }
    }
</style>